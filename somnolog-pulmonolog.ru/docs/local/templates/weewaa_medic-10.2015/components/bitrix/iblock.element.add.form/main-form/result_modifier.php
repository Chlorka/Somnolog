<?/*
$newArr = array();
$newArr[0] = 2;
    foreach($arResult["PROPERTY_LIST"] as $prop) {
        if($prop != 2) {
            $newArr[] = $prop;
        }
    }
unset($arResult["PROPERTY_LIST"]);
$arResult["PROPERTY_LIST"] = $newArr;
*/?>